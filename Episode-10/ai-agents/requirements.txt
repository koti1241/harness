# No external dependencies needed!
# All agents use Python standard library only:
#   - urllib.request (HTTP calls to AI APIs)
#   - json (parse scan results)
#   - argparse (CLI arguments)
#   - subprocess (git commands)
#   - os, sys, datetime (utilities)
#
# Works with Python 3.8+ (no pip install needed)
